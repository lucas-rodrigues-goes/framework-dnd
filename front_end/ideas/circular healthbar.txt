.health-bar-container {
        position: relative;
        margin: 10px;
        width: 120px; /* Slightly larger than the image */
        height: 120px; /* Slightly larger than the image */
    }

    .character-image {
        width: 108px; /* Image width */
        height: 108px; /* Image height */
        border-radius: 50%; /* Circular image */
        position: absolute;
        top: 6px; /* Center image inside the health bar container */
        left: 6px; /* Center image inside the health bar container */
        z-index: 1;
    }

    .health-value {
    position: absolute;
    top: 85%; /* Center vertically */
    left: 50%; /* Center horizontally */
    transform: translate(-50%, -50%); /* Fine-tune positioning */
    font-size: 80%;
    color: white; /* Text color */
    font-weight: bold; /* Optional, makes the text stand out */
    z-index: 3; /* Ensure text is above the health bar */
    background-color: #333;
    border-radius: 10px;
    padding: 0px 5px;
    }

    .health-value {
        z-index:10;
    }

    .bg-circle {
        fill: none;
        stroke: #333; /* Background color of the health bar */
        stroke-width: 5; /* Health bar width */
    }

    .progress-circle {
        fill: none;
        stroke: #ddd; /* Color of the health bar */
        stroke-width: 3; /* Health bar width */
        stroke-linecap: round;
        stroke-dasharray: 345; /* Adjusted for a larger circle */
        stroke-dashoffset: 0;
        transition: stroke-dashoffset 0.3s ease;
    }


        function updateHealth(percentage) {
        const progressCircle = document.getElementById("progressCircle");
        const radius = 55; // Matches the "r" attribute of the circle
        const circumference = 2 * Math.PI * radius;
        const offset = circumference - (percentage / 100) * circumference;

        progressCircle.style.strokeDashoffset = offset;
    }


    <div class="health-bar-container">
    <svg viewBox="0 0 120 120" class="health-bar">
      <circle class="bg-circle" cx="60" cy="60" r="55"></circle>
      <circle class="progress-circle" cx="60" cy="60" r="55" id="progressCircle"></circle>
    </svg>
    <img src="{getTokenPortrait(108,getImpersonated())}" alt="Character" class="character-image" />
    <div class="health-value"><span id="health">0</span> / <span id="max-health">10</span></h3>
  </div>